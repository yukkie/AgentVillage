# GameData — データギャップ管理

Issue #312 の責務：Milestone 1/2 実装中に発覚した「実ログにないフィールド」「フロントエンドが必要だが Python 側が未出力のデータ」を記録し、対応方針を追跡する。

---

## ギャップ一覧

### #338 で発覚したギャップ（ゲーム一覧画面）

| フィールド | 用途 | 現状 | 対応方針 |
|---|---|---|---|
| `village_name` | ゲームカードのタイトル | ❌ ログに存在しない | ❌ スタブ固定（`session_id` で代替） |
| `rule` | ゲームカードのルール表示 | ❌ ログに存在しない | ❌ スタブ固定（`'—'` で代替） |
| `desc` | ゲームカードの1行説明 | ❌ ログに存在しない | ❌ スタブ固定（空文字で代替） |
| `votes` | ゲームカードの投票数 | ❌ ログに存在しない | ❌ スタブ固定（`0` で代替） |
| `comments` | 観戦コメント数 | ❌ ログに存在しない | ❌ スタブ固定（`0` で代替） |
| `hot` | 注目フラグ（「注目」タブのフィルター条件） | ❌ ログに存在しない | ❌ スタブ固定（`false` で代替） |
| `live` | 進行中フラグ（LIVE表示・「注目」タブのフィルター条件） | ❌ ログに存在しない（完了済みアーカイブのみ） | ❌ スタブ固定（`false` で代替） |

上記フィールドは `spectator_log.jsonl` / `agents/*.json` のいずれにも存在しない。
Milestone 3（FastAPI / DB 導入後）に改めて対応方針を検討する。

### #338 で確認したスタブUI（データギャップとは別）

以下は `GameCard` コンポーネント内にハードコードされているが、対応するデータ・機能がない UI 要素。
非表示にするか仕様を確定するかは未定。

**GameCard（中央フィード）**

| UI要素 | 現状 | 対応方針 |
|---|---|---|
| ▲▼ upvote ボタンと数値 | `votes: 0` 固定。ボタンはアクションなし | 仕様未定（ソーシャル機能） |
| 提供: RunVillage | ハードコード文字列 | ゲームホスト情報として定義が必要 |
| 💬 観戦コメント数 | `comments: 0` 固定 | 仕様未定（ソーシャル機能） |
| 👁 同時観戦数 | `viewers: 0` 固定 | #319 LIVE観戦実装時に対応 |
| ⤓ ログDL / ★ 保存 / ↗ 共有 | 機能なしのダミーボタン | 仕様未定 |
| 並び: ▾ Hot | ハードコード文字列、ソートは未実装 | 仕様未定 |
| LIVE バナー（緊迫バナー） | `liveGame` がある場合に表示するが内容はハードコード | #319 LIVE観戦実装時に対応 |

**左サイドナビ（LeftPane）**

| UI要素 | 現状 | 対応方針 |
|---|---|---|
| マイページ（ホーム / ウォッチ中 / 履歴 / 自分のbot） | リンクなしのダミーナビ | 仕様未定（ユーザー機能） |
| 進行中の村 `3` | ハードコード数値 | #319 LIVE観戦実装時に対応 |
| カテゴリ（村人陣営勝 / 狼陣営勝 / 研究村 / 解説付き） | クリックしてもフィルターなし | 仕様未定 |
| ルール（標準11人 / 拡張15人 / 妖狐入り / 短期戦） | クリックしてもフィルターなし | 仕様未定 |
| 注目エージェント / 勝率 | `stub/gameList.js` の `TOP_AGENTS` スタブ固定 | #337 実データ集計で対応予定 |

**右サイドウィジェット（RightPane）**

| UI要素 | 現状 | 対応方針 |
|---|---|---|
| 📅 次回開催（第14回「夜霧の灯台」） | 完全ハードコード | 仕様未定 |
| リマインダー登録ボタン | アクションなし | 仕様未定（通知機能） |
| 🏆 今週の勝率トップ | `stub/gameList.js` の `TOP_AGENTS` スタブ固定 | #337 実データ集計で対応予定 |
| 📰 観戦コミュニティ | `stub/gameList.js` の `COMMUNITY_POSTS` スタブ固定 | 仕様未定（コミュニティ機能） |

**TopBar**

| UI要素 | 現状 | 対応方針 |
|---|---|---|
| 検索ボックス | 入力欄のみ、検索機能なし | 仕様未定 |
| 通知 3 | ハードコード数値、アクションなし | 仕様未定（通知機能） |
| ＋ 自分のbotを参加させる | アクションなし | 仕様未定（bot参加機能）|

**新しい村を作るフォーム（NewVillageForm）**

| UI要素 | 現状 | 対応方針 |
|---|---|---|
| 村を作るボタン | フォームを閉じるだけでゲームを起動しない | `main.py` 起動連携が必要（仕様未定） |

### SpectatorScreen（観戦画面）のスタブUI

#### データギャップ（実ログに存在しないフィールド）

| フィールド | 用途 | 現状 | 対応方針 |
|---|---|---|---|
| `reasoning` | 発言前の思考ログ（spectator限定表示） | ✅ `spectator_log.jsonl` の speech / wolf_chat 行の `reasoning` フィールドに格納（Architecture.md §2.4）。`parseGameData.js` は `event.reasoning` を直接読む。旧ログの `[THINK]` 別行は read 側フォールバックで結合 | ✅ 実装済み（スタブ不要） |
| `claimed_role` | CO（カミングアウト）フラグ | ✅ `spectator_log.jsonl` の speech 行に存在（CO は speech に統合。`co_announcement` 別イベントは廃止予定、旧ログのみフォールバックで対応） | ✅ 実装済み（スタブ不要） |
| `reply_to` | 返信先 speech_id | ✅ `spectator_log.jsonl` の speech 行に存在（返信なし時は `null`） | ✅ 実装済み（スタブ不要） |
| `speech_id` | 発言の連番ID（日内） | ✅ `spectator_log.jsonl` の speech 行に存在 | ✅ 実装済み（スタブ不要） |

#### スタブUI（データ・機能が未実装の UI 要素）

**TopBar（SpectatorScreen）**

| UI要素 | 現状 | 対応方針 |
|---|---|---|
| REPLAY / LIVE バッジ | `REPLAY` ハードコード。LIVE時の切り替えなし | #319 LIVE観戦実装時に対応 |
| 同時観戦 142 | ハードコード数値 | 仕様未定（ソーシャル機能） |
| ⤓ 全ログDL | アクションなしのダミーボタン | 仕様未定 |
| ★ 応援 | アクションなしのダミーボタン | 仕様未定（ソーシャル機能） |

**左ペイン（タイムライン）**

| UI要素 | 現状 | 対応方針 |
|---|---|---|
| 第N日の小見出し（初日 / 荒れる / 進行中） | `stub/spectator.js` の `NIGHT_RESULTS` / `EXEC_RESULTS` スタブ固定。ラベルはハードコード | 実ログ接続時に動的生成が必要 |
| 夜フェーズ ⚰ 死亡者表示 | `stub/spectator.js` の `NIGHT_RESULTS` スタブ固定 | 実ログ接続時に動的生成 |
| 各フェーズの発言数（12 発言） | ハードコード | 実ログから集計が必要 |
| 投票・処刑の処刑対象 | `stub/spectator.js` の `EXEC_RESULTS` スタブ固定 | 実ログ接続時に動的生成 |
| 夜フェーズ（完了 / 進行中）ラベル | ハードコードロジック（`d < 3 ? '完了' : '進行中'`） | 実ログのゲーム状態から判定が必要 |

**中央フィード**

| UI要素 | 現状 | 対応方針 |
|---|---|---|
| Day N 議論 `3:47 経過 / 残り 4:13` | セッションIDなし時にハードコード。セッションIDあり時はsessionIdを表示 | #319 LIVE観戦実装時に対応 |
| 投票確定 `6/9` | ハードコード | 実ログ接続時に集計が必要 |
| ⇅ 新しい順 | アクションなしのダミーボタン（ソートなし） | 仕様未定 |
| 🔍 検索 | アクションなしのダミーボタン | 仕様未定 |
| VoteDetail（投票内訳カード） | `day === 1` の場合のみ表示、`stub/spectator.js` の `VOTE_TABLE_D1` / `EXEC_RESULTS` スタブ固定 | 実ログ接続時に各日の投票内訳を動的生成 |
| エージェント alias `#NN` | `name.charCodeAt(0) % 99` でハードコード計算 | 仕様未定（エージェントID体系が必要） |
| セッションIDなし時の固定フィード（処刑・夜フェーズ行） | `!sessionId` 分岐でハードコードイベントを追加 | スタブ確認用。実装後は除去 |

**右ペイン（参加エージェント一覧）**

| UI要素 | 現状 | 対応方針 |
|---|---|---|
| 容疑度メーター | `name.charCodeAt(0) * 13 % 100` でハードコード計算 | エージェントの推論ログ（`thought`）から集計が必要。仕様未定 |
| 占COバッジ（ Ren / Nox ） | エージェント名のハードコード条件分岐 | `claimed_role` フィールド実装後に動的生成 |
| 発言数 | `name.length * 3 + 4` でハードコード計算 | 実ログから集計が必要 |
| カミングアウト状況ボード | `stub/spectator.js` の `ROLE_ASSIGNMENT` スタブ固定。内容ハードコード | `claimed_role` フィールド実装後に動的生成 |
| 死亡者の死因（Day1 夜・襲撃 等） | エージェント名のハードコード条件分岐 | `NIGHT_RESULTS` / `EXEC_RESULTS` 実ログ接続時に動的生成 |
| 夜の行動・推測リスト | `stub/spectator.js` の `ACTIONS_TIMELINE` スタブ固定 | 実ログ接続時に動的生成。`thought` / 夜フェーズログが必要 |

---

### AgentDetailScreen（エージェント詳細画面）のスタブUI

AgentDetailScreen は現在 `stub/agentDetail.js` のデータのみを参照しており、実ログ・実アーカイブへの接続はない。

#### データギャップ

| フィールド | 用途 | 現状 | 対応方針 |
|---|---|---|---|
| blurb | エージェントの1行プロフィール | ✅ `frontend/public/config/agents.json` の `blurb`（英語）で実データ化済み（#519） | `/config/agents.json` を `fetchAgentConfig()` で fetch・`parseBlurb()` で抽出。fetch 失敗／未定義は `—` フォールバック。日本語化は将来の別 Issue（`persona_short` は使わない） |
| 通算戦績（wins / games / cheers） | ヒーローヘッダーの統計表示 | ❌ アーカイブから集計されていない | `stub/agentDetail.js` の `AGENT_STATS` スタブ固定。#337 実データ集計で対応予定 |
| 疑い・信頼スコア | 疑いマトリクス表示 | ❌ 実ログに存在しない | `getSuspicionMatrix()` が `charCodeAt` ベースのハッシュで生成。エージェントの `thought` から推定する設計が必要 |
| 夜の行動ログ | 夜の行動タブ | ✅ `spectator_log.jsonl` に観戦者向けイベントとして存在 | `stub/agentDetail.js` の `NIGHT_ACTIONS` スタブ固定。実ログ接続が必要 |

#### スタブUI

**TopBar（AgentDetailScreen）**

| UI要素 | 現状 | 対応方針 |
|---|---|---|
| LIVE観戦中バッジ | ハードコード（常に LIVE 表示） | #319 LIVE観戦実装時に対応 |
| ⤓ プロファイルJSON | アクションなしのダミーボタン | 仕様未定 |
| ★ ウォッチ | アクションなしのダミーボタン | 仕様未定（ソーシャル機能） |
| パンくず（r/agent-jinrou · 第13回 桜霞） | ハードコード | 実ゲームデータと接続が必要 |

**左ペイン（エージェント選択）**

| UI要素 | 現状 | 対応方針 |
|---|---|---|
| エージェント一覧・生死状態 | `stub/agentDetail.js` の `ALL_AGENTS` / `DEAD_AGENTS` スタブ固定 | 実アーカイブ接続時に動的生成 |
| 並べ替えボタン（発言数↓ / 容疑度↓ / 役職別） | クリックしてもソートなし | 仕様未定 |
| statusDot（生存インジケーター） | 表示はあるが色分けロジックなし | 仕様未定 |

**ヒーローヘッダー**

| UI要素 | 現状 | 対応方針 |
|---|---|---|
| 第13回・桜霞村 | ハードコード | 実ゲームデータと接続が必要 |
| 生存中 · Day 2 / 死亡 | `DEAD_AGENTS` スタブ固定 | 実アーカイブ接続時に動的生成 |
| 応援スコア `cheers` | `stub/agentDetail.js` スタブ固定 | 仕様未定（ソーシャル機能） |

**概要タブ**

| UI要素 | 現状 | 対応方針 |
|---|---|---|
| 現在の目標 | エージェントによらずハードコード1文 | `thought` フィールド実装後に動的生成 |
| 直近の推論 | `stub/agentDetail.js` の `THOUGHTS` スタブ固定（Nox のみデータあり） | `thought` フィールド実装後に動的生成 |

**過去の戦績タブ**

| UI要素 | 現状 | 対応方針 |
|---|---|---|
| 戦績レコード（#9〜#13） | エージェントによらずハードコード5件 | #337 実データ集計で対応予定 |

---

### 「注目」タブの仕様未定

`GameListScreen` の「▶ 注目」タブは `hot || live` でフィルターしているが、`hot` / `live` ともに現行アーカイブでは常に `false` のため表示件数が 0 になる。
「注目」タブの選定ロジック（閲覧数・コメント数・管理者フラグ等）は仕様未定。
暫定対応として、デフォルト表示タブを「完了」に変更済み（#338）。
「注目」タブの仕様は FastAPI / LIVE 観戦（#319）実装時に合わせて設計する。

---

## 凡例

- **発見Issue**: このギャップを発見した実装 Issue
- **現状**: 実ログ（`spectator_log.jsonl` / `state_archive/`）における現在の状態
- **対応方針**: Milestone 2 以降の実データ接続時に取るべきアクション
